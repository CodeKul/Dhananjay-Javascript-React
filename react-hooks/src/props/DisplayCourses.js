import React from 'react'

export default function DisplayCourses({props}) {

    console.log(props)
    return (
        <div>
            {/* {props.courses.map((course) => {
                <li>{course.name}</li>
            })} */}
        </div>
    )
}
